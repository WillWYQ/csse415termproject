name: TOC Generator

on:
  push:
    branches:
      - main
    paths:
      - '**.md'

jobs:
  generate-toc:
    runs-on: ubuntu-latest
    steps:
      - name: TOC Generator
        uses: technote-space/toc-generator@v4.3.1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
